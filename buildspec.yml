version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing backend dependencies..."
      - cd backend && npm install

  build:
    commands:
      - echo "Build phase complete — nothing to compile for this project."

artifacts:
  files:
    - '**/*'
  discard-paths: no
